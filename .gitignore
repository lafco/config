# Pi agent — sensitive files
pi/.pi/agent/auth.json
pi/.pi/agent/models-store.json
pi/.pi/agent/sessions/
pi/.pi/agent/bin/
pi/.pi/agent/npm/

# jira-flow — credenciais locais
pi/.pi/agent/extensions/jira-flow/.env
pi/.pi/agent/secrets.json
pi/.pi/agent/secrets.json.bak*

# ahg-request — extensões, doc e skills ficam só nesta máquina
# (rotas internas do PW2, do módulo de férias e do folgas-api + códigos de empresa/funcionário)
# Sem barra final: os caminhos reais são symlinks para ~/repos/ahg-request
pi/.pi/agent/extensions/ahg-request
pi/.pi/agent/extensions/ahg-request.md
pi/.pi/agent/skills/pw2-request
pi/.pi/agent/skills/folgas-request

# opensearch_request — extensão, doc e skill ficam só nesta máquina
# (instância interna de logs + index patterns internos)
# Sem barra final: os caminhos reais são symlinks para ~/repos/opensearch-request
pi/.pi/agent/extensions/opensearch
pi/.pi/agent/extensions/opensearch-request.md
pi/.pi/agent/skills/opensearch-request

# Editor / system
*~
*.swp
*.swo
.DS_Store

# Zellij runtime

# Herdr runtime files
herdr/.config/herdr/*.log
herdr/.config/herdr/session.json
herdr/.config/herdr/.plugins.lock
herdr/.config/herdr/release-notes.json

# dot CLI runtime (falhas de instalação)
packages/failed_tools.txt


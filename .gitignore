# Pi agent — sensitive files
pi/.pi/agent/auth.json
pi/.pi/agent/models-store.json
pi/.pi/agent/sessions/
pi/.pi/agent/bin/
pi/.pi/agent/npm/

# jira-flow — credenciais locais
pi/.pi/agent/extensions/jira-flow/.env
pi/.pi/agent/secrets.json

# pw2_request — extensão, doc e skill ficam só nesta máquina
# (rotas internas do PW2 e do módulo de férias + códigos de empresa/funcionário)
# Sem barra final: os caminhos reais são symlinks para ~/repos/pw2-request
pi/.pi/agent/extensions/pw2
pi/.pi/agent/extensions/pw2-request.md
pi/.pi/agent/skills/pw2-request

# Editor / system
*~
*.swp
*.swo
.DS_Store

# Zellij runtime

# Herdr runtime files
herdr/.config/herdr/*.log
herdr/.config/herdr/session.json
herdr/.config/herdr/.plugins.lock
herdr/.config/herdr/release-notes.json

# dot CLI runtime (falhas de instalação)
packages/failed_tools.txt


# hardware-configuration.nix — PLACEHOLDER (não é o hardware real!)
#
# Este arquivo é gerado pelo instalador na máquina de destino. Durante a
# instalação do NixOS, substitua o conteúdo com:
#
#   sudo nixos-generate-config --show-hardware-config \
#     > ~/dotfiles/nixos/hosts/hmpc/hardware-configuration.nix
#
# O comando grava os fileSystems reais (UUIDs dos discos), initrd (luks/btrfs)
# e módulos de kernel detectados nesta máquina específica.
{ ... }:
{ }
